# https://taskfile.dev

version: "3"

silent: true

vars:
  packages: ("User")

tasks:
  default:
    cmds:
      - task --list

  lint:
    desc: Lint packages with Helm
    deps: [has:helm]
    cmds:
      - |
        pkgs={{.packages}}
        for i in "${pkgs[@]}"
        do 
          task lint:package pkg=$i; 
        done

  lint:package:
    cmds:
      - helm lint {{ .pkg }}

  has:helm:
    preconditions:
      - sh: command -v helm
        msg: Install Helm first
